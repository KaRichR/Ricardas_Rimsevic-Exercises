function getElementsByClassName(className) {
    const elements = document.getElementsByClassName(className);
    return elements;
  }
  
  const elements = getElementsByClassName("elementas1");
  console.log(elements);
  